v class="team ${t.name===state.config.mine?"mine":""}">
      <div class="team-top">
        <div><div class="team-name">${esc(t.name)}${t.name===state.config.mine?" · TU":""}</div><div class="slots">${slots}</div></div>
        <div class="credits">${left}</div>
      </div>
      <div class="bar"><i style="width:${pct}%"></i></div>
      <details><summary>Rosa (${roster.length})</summary><div class="roster">${rosterHtml}</div></details>
    </div>`;
  }).join("");
}

function filteredPlayers(){
  const q=byId("search").value.trim().toLowerCase();
  const rf=byId("roleFilter").value;
  const tf=byId("tierFilter").value;
  const sf=byId("statusFilter").value;
  const sort=byId("sortBy").value;
  let arr=PLAYERS.filter(p=>{
    const sold=soldEntry(p.name);
    if(q && !p.name.toLowerCase().includes(q) && !p.club.toLowerCase().includes(q)) return false;
    if(rf && p.role!==rf) return false;
    if(tf && p.tier!==tf) return false;
    if(sf==="available" && sold) return false;
    if(s